# Supabase 配置
VITE_SUPABASE_URL=https://rqfgdeddzokontbdcsna.supabase.co
VITE_SUPABASE_ANON_KEY=从 Supabase Dashboard → API 获取（anon public key）

# API Key 加密密钥（64位hex字符串，256-bit AES-GCM）
# 生成方式：openssl rand -hex 32
# ⚠️ 绝对不能提交到 git！不能在前端代码中硬编码！
VITE_ENCRYPTION_KEY=运行 openssl rand -hex 32 生成
